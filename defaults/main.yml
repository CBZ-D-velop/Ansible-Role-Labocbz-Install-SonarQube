---

install_sonarqube__postgres_sonarqube_login: "sonarqube"
install_sonarqube__postgres_sonarqube_password: "KDOEKD930DKDOEK3"

install_sonarqube__container_name: "sonarqube"
install_sonarqube__postgresql_container_name: "postgresql"

install_sonarqube__web_host: "0.0.0.0"
install_sonarqube__web_port: 9000
